{
  "kind": "collectionType",
  "collectionName": "featured_projects",
  "info": {
    "singularName": "featured-project",
    "pluralName": "featured-projects",
    "displayName": "Featured Project"
  },
  "options": { "draftAndPublish": true },
  "attributes": {
    "images": {
      "type": "media",
      "multiple": true,
      "allowedTypes": ["images"],
      "required": true,
      "description": "Todas las fotos del proyecto — la primera se usa como portada"
    },
    "order": {
      "type": "integer",
      "required": true,
      "description": "Posición (1-5)"
    },
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text",
      "description": "Descripción breve del proyecto"
    }
  }
}